# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: self-hosted

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2

      - uses: NuGet/setup-nuget@v1.0.5

      #- name: Setup UiPath CLI
      #  run: 'nuget.exe install UiPath.CLI -Version 1.0.7985.19721 -Source https://www.myget.org/F/uipath-dev/api/v3/index.json'
        
      - name: UiPath Pack
        run: 'C:\actions-runner\_work\UiPath.CLI.1.0.7985.19721\lib\net461\uipcli.exe package pack "$GITHUB_WORKSPACE\DemoForVera\ProjectFolder\project.json" -o "C:\" --libraryOrchestratorUrl https://cloud.uipath.com/hppcntv --libraryOrchestratorTenant Mikaels_RnD --libraryOrchestratorUsername hppcntv --libraryOrchestratorPassword tuVh9N_P5V-4yCgpu1vm3Q11HU0ZC1m3o9B_p-KoHcunm --autoVersion --outputType Process' 
    
